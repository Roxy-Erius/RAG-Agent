# Grafana 看板自动加载：启动时扫描 /var/lib/grafana/dashboards 下的 json
apiVersion: 1

providers:
  - name: 'rag-dashboards'
    orgId: 1
    folder: 'RAG Agent'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
